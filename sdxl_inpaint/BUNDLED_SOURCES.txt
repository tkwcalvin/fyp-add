stable_diffusion.py is copied from FYP_JAYHF1
(in this workflow, the sticker-fuse subcommand is commonly used;
the demo subcommand may depend on sample assets from the original repo).

Sync example:

  cp /path/to/fyp/FYP_JAYHF1/stable_diffusion.py /path/to/fyp/object_insert_workflow/sdxl_inpaint/

Environment: SDXL_MODEL_DIR / MODEL_DIR should still point to a local snapshot
containing model_index.json (same behavior as the original script).
